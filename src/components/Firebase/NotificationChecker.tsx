const NotificationChecker = () => {}
// NOTE For the future to create a notification e.g when user doesn't login for a certain time or e.g to send notifiaction on each day in the morning

export default NotificationChecker
